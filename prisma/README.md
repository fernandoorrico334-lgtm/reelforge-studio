# Prisma Placeholder

Esta pasta foi reservada para a futura camada de persistencia com SQLite + Prisma.

Estrutura esperada na proxima etapa:

- `schema.prisma`
- `migrations/`
- `dev.db` como banco SQLite local inicial

Ainda nao existe schema, migracao ou cliente nesta fase porque o foco atual e arquitetura, contratos e documentacao.

